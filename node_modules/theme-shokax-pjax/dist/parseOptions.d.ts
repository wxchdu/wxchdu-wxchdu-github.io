import { PjaxOptions } from "./types";
export default function pjax({ elements, selectors, switches, switchesOptions, history, scrollTo, scrollRestoration, cacheBust, timeout, currentUrlFullReload, }?: Partial<PjaxOptions>): PjaxOptions;
